    # TODO: Fix GitHub Pages 400 error

- [x] Verify current file names in repo root.
- [x] Rename `beauty-atelier-salon (2).html` -> `index.html` (avoid spaces/parentheses).

- [ ] Confirm `index.html` still references `styles.css` and `script.js` with correct relative paths.
- [ ] Re-upload/push to GitHub.
- [ ] Update/confirm GitHub Pages URL to the root page (or correct path).
- [ ] Open the Pages URL to confirm the 400 error is resolved.

